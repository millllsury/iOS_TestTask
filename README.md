# iOS Test Task

The project contains the general basic structure of the application and is prepared for two separate interface implementations:

- `uikit-implementation` — UIKit version
- `swiftui-implementation` — SwiftUI version

## Current status
The main branch contains the overall project structure:
- base models
- mock data
- prepared assets
- foundation for further screen implementation

## Tests
Basic unit tests have been added at the level of the overall project structure and core logic, common to the future `uikit-implementation` and `swiftui-implementation` branches.


## Implementations
To view a specific implementation, use the separate branches:

```bash
git checkout uikit-implementation
git checkout swiftui-implementation
